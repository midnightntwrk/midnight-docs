name: Build

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Activate Yarn 4 via Corepack (and force alias)
        shell: bash
        run: |
          set -euxo pipefail

          # Hard-activate the exact Yarn you want
          corepack enable
          corepack prepare yarn@4.10.3 --activate

          # Make sure any future `yarn` calls in this job use Corepack's yarn
          echo 'export YARN_IGNORE_PATH=1' >> "$GITHUB_ENV"
          {
            echo 'yarn() {'
            echo '  command corepack yarn "$@";'
            echo '}'
          } >> "$BASH_ENV"
          source "$BASH_ENV"

          node -v
          command -v corepack
          corepack yarn -v
          # This will now also print 4.x because of the alias:
          yarn -v

      - name: Cache Yarn downloads
        uses: actions/cache@v4
        with:
          path: |
            .yarn/cache
            .yarn/install-state.gz
          key: ${{ runner.os }}-yarn4-${{ hashFiles('**/yarn.lock') }}

      - name: Install dependencies (Yarn 4)
        env:
          YARN_IGNORE_PATH: "1"
          YARN_ENABLE_HARDENED_MODE: "0"
        run: |
          # If yarn.lock exists, use immutable; otherwise plain install
          if [ -f yarn.lock ]; then
            yarn install --immutable
          else
            yarn install
          fi

      - name: Build
        env:
          YARN_IGNORE_PATH: "1"
        run: yarn build
